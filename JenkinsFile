pipeline {
    agent('any')
    stages {
        stage('git-clone') {
            steps {
                git branch: 'main', url: 'https://github.com/manoharc07/scratch'
            }
        }
        stage('Creating-Accounts'){
            steps{
               bat '''
                    echo "Testing"
               '''
            
            }
        }
        
    }
}
